# Visual Verification Notes

## 2026-08-14 — Availability, client memory, and announcement

The admin availability calendar renders correctly with multi-day selection, exact start/end time controls, and clear open/closed/selected states. The Clients and Notices workspaces render correctly in the admin dashboard.

The public booking form shows the intended no-open-dates state until Isaac opens dates in the Availability workspace. The active vacation announcement loads successfully from the public API and renders in the hero area. On the current desktop verification it is positioned below the hero copy rather than beside it; the responsive layout needs one CSS adjustment to place it in the requested right-side column on desktop while retaining a stacked mobile layout.
